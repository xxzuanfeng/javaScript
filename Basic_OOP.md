# Learn  OOP

```js
class Computer {
    constructor () {
        this.ram = 16;
    }
    
    addRam(amount) {
        this.ram += amount;
    }
};							//类
const myComputer = new Computer();       //实例化对象

canvas.getContext("2d");				//创建一个平面
ctx.fillStyle = "#99c9ff";				//元素填充颜色
ctx.fillRect(x, y, width, height)		//元素画形状
```



